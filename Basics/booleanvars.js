//--- Boolean JS Data Type ----

let age = 21;
let isAdult = age >= 18; //Comparison operators yield a boolean value
console.log(isAdult);

let isNotAdult = !isAdult; //complement
console.log(isNotAdult);

let fname = "user";
console.log(fname === "user"); //strings can be compared too

//Exercise
//Create 2 boolean vars - isCheap and isExpensive
//  the price is cheap if its below 100
//  its expensive is its above 100
//Create a price var and store a number
//Using comparion operators, store the correct values in isCheap and isExpensive
